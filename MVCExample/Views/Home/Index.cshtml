@{
    // View code goes here.
}
@using (Html.BeginFrame(new { posn = "0 0 0" }))
{
	// Begin a quad element. This element will appear inside the <frame> element.
	using (Html.BeginQuad(new { posn="0 0 0", style = "", substyle = "" }))
	{
	}
}